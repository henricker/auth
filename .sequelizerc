const path = require("path");

module.exports = {
    
    'config': path.resolve(__dirname, 'src', 'config', 'dbConfig.json'),
    'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations')
}